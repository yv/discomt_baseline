[]
conf_en = english.yml
conf_de = german.yml
conf_fr = french.yml
[%{corpus}.%{src}-%{tgt}.none%{noneval}.predictions.txt]
dep.data = %{corpus}.%{src}-%{tgt}.data
dep.conf = %{locals()['conf_'+tgt]}
recipe = python discomt_baseline.py --conf %{conf} --removepos \
  --none-penalty %{noneval} %{data} > %{target} 
